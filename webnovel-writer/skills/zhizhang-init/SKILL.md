---
name: zhizhang-init
description: 织章兼容入口，等同于 webnovel-init，用于深度初始化网文项目。
allowed-tools: Read Write Edit Grep Bash Task AskUserQuestion WebSearch WebFetch
---

# 织章兼容入口 /zhizhang-init

这是 `webnovel-init` 的新命名别名。请先阅读并遵循 `../webnovel-init/SKILL.md` 中的完整深度初始化流程。

执行原则：
- 新品牌对外使用 `/zhizhang-init`
- 旧兼容命令 `/webnovel-init` 继续可用
- 初始化产物、结构和闸门规则以 `webnovel-init` 的规范为准
