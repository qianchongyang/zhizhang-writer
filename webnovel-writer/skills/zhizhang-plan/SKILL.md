---
name: zhizhang-plan
description: 织章兼容入口，等同于 webnovel-plan，用于生成卷级与章节大纲。
allowed-tools: Read Write Edit Grep Bash Task AskUserQuestion WebSearch WebFetch
---

# 织章兼容入口 /zhizhang-plan

这是 `webnovel-plan` 的新命名别名。请先阅读并遵循 `../webnovel-plan/SKILL.md`。

执行原则：
- 新品牌对外使用 `/zhizhang-plan`
- 旧兼容命令 `/webnovel-plan` 继续可用
- 规划流程、输入输出和约束以 `webnovel-plan` 的规范为准
