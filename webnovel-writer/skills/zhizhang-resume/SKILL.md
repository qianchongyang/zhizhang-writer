---
name: zhizhang-resume
description: 织章兼容入口，等同于 webnovel-resume，用于恢复中断的写作任务。
allowed-tools: Read Bash AskUserQuestion
---

# 织章兼容入口 /zhizhang-resume

这是 `webnovel-resume` 的新命名别名。请先阅读并遵循 `../webnovel-resume/SKILL.md`。

执行原则：
- 新品牌对外使用 `/zhizhang-resume`
- 旧兼容命令 `/webnovel-resume` 继续可用
- 恢复流程、断点判断和安全策略以 `webnovel-resume` 的规范为准
