---
name: zhizhang-adjust
description: 织章兼容入口，等同于 webnovel-adjust，用于动态调整大纲、插入副本和修正冲突。
allowed-tools: Read Write Edit Grep Bash Task AskUserQuestion WebSearch WebFetch
---

# 织章兼容入口 /zhizhang-adjust

这是 `webnovel-adjust` 的新命名别名。请先阅读并遵循 `../webnovel-adjust/SKILL.md` 中的完整流程与约束。

执行原则：
- 新品牌对外使用 `/zhizhang-adjust`
- 旧兼容命令 `/webnovel-adjust` 继续可用
- 具体实现、参数和产出以 `webnovel-adjust` 的规范为准
