---
name: zhizhang-query
description: 织章兼容入口，等同于 webnovel-query，用于查询角色、伏笔、状态与节奏信息。
allowed-tools: Read Grep Bash AskUserQuestion
---

# 织章兼容入口 /zhizhang-query

这是 `webnovel-query` 的新命名别名。请先阅读并遵循 `../webnovel-query/SKILL.md`。

执行原则：
- 新品牌对外使用 `/zhizhang-query`
- 旧兼容命令 `/webnovel-query` 继续可用
- 查询类型、参数和输出规则以 `webnovel-query` 的规范为准
